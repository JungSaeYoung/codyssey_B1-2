# 실험 절차서 — 3대 장애 재현 & 검증

> 본 문서는 [reports/](reports/) 의 3개 리포트가 어떤 순서와 환경변수 조합으로 수집된 증거인지를 추적할 수 있도록 정리한 절차서다.
>
> 🤖 **자동 수행:** 아래 절차(§1~§4)는 [src/experiments/](src/experiments/) 의 스크립트로 사람 손 없이 그대로 돌릴 수 있다.
> `bash src/experiments/00_run_experiments.sh all` (개별: `01_oom.sh` … `04_scheduling.sh`). 각 파일이 절차서의 한 실험에 1:1 대응한다.

---

## 0. 공통 사전 조건

- B1-1 환경 구축 완료 (`agent-admin` 계정, `$AGENT_HOME` 셋업, monitor.sh + crontab 매분 등록)
- `agent-leak-app` 바이너리가 `$AGENT_HOME/agent-leak-app` 에 배치됨 (`src/06_deploy_app_and_scripts.sh` 가 `$AGENT_HOME` 루트에 install; `$AGENT_HOME/bin/` 에는 `monitor.sh`·`report.sh`·`archive_logs.sh` 관제 스크립트만)
- `secret.key` 내용 = `agent_api_key_test`
- 매 실험 사이에 이전 PID 잔존 여부 점검 — 잔존 시 `kill <pid>` (SIGKILL 은 마지막 수단)

```bash
# 베이스라인 환경변수 (정상 가동용)
export MEMORY_LIMIT=512
export CPU_MAX_OCCUPY=10
export MULTI_THREAD_ENABLE=false
```

> ⚠ 이 앱은 부팅 시 Resource Check 결과로 **시나리오를 하나 고른다**(실측).
> `MEMORY_LIMIT<=256` → 메모리 누수 / `MEMORY_LIMIT>256` & `CPU_MAX_OCCUPY>50` → CPU 과점유 /
> `MULTI_THREAD_ENABLE=true` → 데드락 / 셋 다 안전 → `[Healthy System Monitoring]`.
> 그래서 베이스라인의 `CPU_MAX_OCCUPY` 는 95 가 아니라 **10** 이어야 한다.
> 95 로 두면 "정상 가동" 이 아니라 CPU 과점유 시나리오가 선택돼 34초 만에 죽는다(exit 143).
> 각 실험은 **바꾸려는 변수 하나만** 움직이고 나머지는 안전값으로 고정한다.

---

## 1. OOM 실험

### 1-1. Before 조건 (장애 재현)

```bash
export MEMORY_LIMIT=256
export CPU_MAX_OCCUPY=10
export MULTI_THREAD_ENABLE=false
nohup ./agent-leak-app > $AGENT_LOG_DIR/agent_app.out 2>&1 &   # stdout(부트 시퀀스)
# 앱 자체 로그는 앱이 직접 $AGENT_LOG_DIR/agent_app.log 에 append 한다 (실행 전 비워둘 것)
echo $! > /tmp/agent.pid
```

### 1-2. 관측 (실측 34초 — 3초마다 PROC_RSS +25MB)

- `tail -f /var/log/agent-app/monitor.log` — **`PROC_RSS`** 선형 증가 확인 (라이브 관찰). `SYS_MEM` 은 거의 안 움직인다
- `tail -f $AGENT_LOG_DIR/agent_app.log` — `[MemoryGuard] Memory limit exceeded` / `Self-terminating process <PID>` 라인 출현
- 종료 후 `ps -p $(cat /tmp/agent.pid)` → 사라짐 확인

**프로세스 RSS 추세 수치만 추출** (리포트의 Evidence 표/그래프용):

> monitor.log 한 줄 포맷 —
> `[TS] PID:n SYS_CPU:x% SYS_MEM:y% PROC_CPU:z% PROC_RSS:wMB DISK_USED:d%`
> `SYS_*` 는 **호스트 전체**, `PROC_*` 가 **대상 프로세스** 다.
> B1-2-A1(프로세스 물리 메모리 증가)의 근거는 `SYS_MEM` 이 아니라 **`PROC_RSS`** 다
> (앱 heap 이 0→300MB 로 올라도 `SYS_MEM` 은 35.5%→35.6% 로 거의 안 움직인다).

```bash
# 해당 PID 의 PROC_RSS(MB) 시계열만 뽑기
PID=$(cat /tmp/agent.pid)
grep "PID:${PID}" /var/log/agent-app/monitor.log | awk -F'PROC_RSS:' '{print $2}' | awk -F'MB' '{print $1}'

# Max/Min·도달 시각·평균 통계 한 번에 (구간 지정 가능)
bash src/report.sh                                  # 전체 구간
bash src/report.sh "2026-08-26 09:02:37" "2026-08-26 09:03:11"   # OOM Before 구간 (실측 34초)
#   → ====== STATISTICS REPORT ====== 의 [Process RSS] Maximum … MB at … 형태로 출력
```

### 1-3. After 조건 (조치 검증)

```bash
export MEMORY_LIMIT=512   # 256 → 512  (CPU_MAX_OCCUPY 는 10 으로 고정)
# 재실행 후 동일 절차로 관측 → 실측: 34초 사망 → 관측 상한 3m00s 까지 종료 없음
#   ※ 512 는 '더 늦게 죽는' 값이 아니라 '이 장애가 재현되지 않는' 값이다.
#     부트 배너가 [ MEMORY ] Limit: 512MB [ OK ] 로 바뀌며 누수 시나리오 자체가 선택되지 않는다.
#     '한도만 늘려 생존이 연장되는' 임시 조치 효과는 100MB(13초) → 256MB(34초) 대조에서 확인된다.
```

### 1-4. 산출물

- [evidence/oom_monitor.log](evidence/oom_monitor.log)
- [evidence/oom_app.log](evidence/oom_app.log)
- [evidence/oom_ps_top.txt](evidence/oom_ps_top.txt)
- 분석: [reports/01_oom_report.md](reports/01_oom_report.md)

---

## 2. CPU 과점유 실험

### 2-1. Before

```bash
export MEMORY_LIMIT=512
export CPU_MAX_OCCUPY=80
export MULTI_THREAD_ENABLE=false
nohup ./agent-leak-app > $AGENT_LOG_DIR/agent_app.out 2>&1 &   # stdout(부트 시퀀스)
# 앱 자체 로그는 앱이 직접 $AGENT_LOG_DIR/agent_app.log 에 append 한다 (실행 전 비워둘 것)
```

### 2-2. 관측

- 프로세스가 **30초 안에** 죽으므로 30초 간격 `top` 으로는 구간을 못 잡는다. 짧은 간격으로 캡처할 것
- `[INFO] [CpuWorker] Current Load: NN%` 가 3초마다 상승 (5.00 → … → 53.72%) ← **앱 자기보고 값**
- `[CRITICAL] [CpuWorker] CPU Threshold Violated! (…%)` 라인 출현 = 보호 종료. 종료 코드 143(=128+15, SIGTERM)
  ※ 이 바이너리는 `WATCHDOG … SIGTERM` 문자열을 찍지 않는다 (실측 확인). 위 라인이 종료 판정 근거다.
- OS 가 본 실제 프로세스 CPU 는 훨씬 작다(`TIME+` 27초간 0.24초). 순간값이 필요하면 누적 평균인
  `ps %CPU` 대신 `/proc/<PID>/stat` 의 `utime+stime` 을 1초 델타로 계산할 것:

```bash
while kill -0 $PID 2>/dev/null; do awk '{print $14+$15}' /proc/$PID/stat; sleep 1; done
```

### 2-3. After

```bash
export CPU_MAX_OCCUPY=10   # 80 → 10  ← 상향이 아니라 '하향'
# 재실행 → 실측: 종료 자체가 사라짐 (관측 상한 3m00s 까지 생존, Peak reached → cooldown 17회 순환)
#   ※ 상향(80→95)은 효과 0 이다. 실측 생존 30초 vs 34초, 종료 코드·로그 동일.
#     종료를 결정하는 선은 CPU_MAX_OCCUPY 가 아니라 부트 배너가 경고하는 고정 50%
#     ('[ CPU ] Limit: 80% [ WARNING: Recommend Under 50% ]') 다. 임계점은 50 과 60 사이.
```

### 2-4. 산출물

- [evidence/cpu_monitor.log](evidence/cpu_monitor.log)
- [evidence/cpu_app.log](evidence/cpu_app.log)
- [evidence/cpu_top_ps.txt](evidence/cpu_top_ps.txt)
- 분석: [reports/02_cpu_report.md](reports/02_cpu_report.md)

---

## 3. Deadlock 실험

### 3-1. Before

```bash
export MEMORY_LIMIT=512
export CPU_MAX_OCCUPY=10            # 95 로 두면 freeze 관측 전에 CpuWorker 가 죽인다
export MULTI_THREAD_ENABLE=true     # ★ 핵심
nohup ./agent-leak-app > $AGENT_LOG_DIR/agent_app.out 2>&1 &   # stdout(부트 시퀀스)
# 앱 자체 로그는 앱이 직접 $AGENT_LOG_DIR/agent_app.log 에 append 한다 (실행 전 비워둘 것)
PID=$!
```

### 3-2. 관측 (실측: 부팅 약 9초 후 무응답 진입)

```bash
ps -ef | grep $PID                 # PID 존재 확인
ps -p $PID -o stat,etime,cmd       # SNl (sleeping + nice + multi-thread)
top -H -bn1 -p $PID                # 스레드 3개, TIME+ 가 0:00.00 에서 전진하지 않음
ps -L -p $PID -o lwp,pcpu,stat,wchan:25,cmd   # 전 스레드 wchan=futex_wait_queue (커널 6.8 심볼명)
stat -c '%s bytes, mtime=%y' $AGENT_LOG_DIR/agent_app.log   # 크기·mtime 이 정지 = 판별력 있는 지표
tail -n 20 $AGENT_LOG_DIR/agent_app.log       # 'WAITING for [...]... (Status: BLOCKED)' 에서 정지
curl --max-time 3 http://127.0.0.1:15034      # 증거로만 보관 — 판별 근거로 쓰지 말 것 (아래 주의)
```

> ⚠ `curl` 은 **판별력이 없다**(실측). 정상 모드에서도 15034 는 LISTEN 만 하고 응답하지 않아
> 두 구간 모두 `timeout, 0 bytes` 로 끝난다. 데드락 판정은
> **① 로그 mtime 정지 ② `ps -L` 전 스레드 `futex_wait_queue` ③ `top -H` TIME+ 누적 정지** 로 한다.

### 3-3. After

```bash
export MULTI_THREAD_ENABLE=false
# 재실행 → 실측: 관측 3m00s 내내 정상 가동 (로그 전진 + PID 생존)
#   ps -L 이 futex_wait_queue 1개(메인=조인 대기) + do_select 2개(워커 동작)로 바뀌고
#   top -H TIME+ 가 0:00.05 / 0:00.79 / 0:00.47 로 전진한다. curl 은 여전히 무응답이므로 판정에 쓰지 않는다.
```

### 3-4. 산출물

- [evidence/deadlock_app.log](evidence/deadlock_app.log)
- [evidence/deadlock_ps_top.txt](evidence/deadlock_ps_top.txt)
- [evidence/deadlock_monitor.log](evidence/deadlock_monitor.log)
- 분석: [reports/03_deadlock_report.md](reports/03_deadlock_report.md)

---

## 4. 보너스 — 스케줄링 추론용 데이터 수집

위 실험 사이사이 **정상 가동 구간**(데드락 미발생, OOM 직전 충분히 떨어진 구간)에서:

```bash
export MULTI_THREAD_ENABLE=false    # ★ true 는 데드락 시나리오라 [Scheduler] 회전 로그가 안 나온다
export MEMORY_LIMIT=512
export CPU_MAX_OCCUPY=10            # 셋 다 안전 → [Healthy System Monitoring] 시나리오
./agent-leak-app &
# 120초간 워커 로그 누적
sleep 120
grep -E '\[[A-Za-z0-9_-]*(Worker|Thread|Scheduler)[A-Za-z0-9_-]*\]' $AGENT_LOG_DIR/agent_app.log > evidence/scheduling_workers.log
top -H -bn1 -p $(pgrep -x agent-leak-app | tail -n1) > evidence/scheduling_top_h.txt
```

> `[Scheduler]` 가 `Thread-A/B/C` 를 회전시키는 블록은 **부팅 직후 약 1.07초 동안 한 번만** 나온다
> (이벤트 21개, 간격 20개가 전부 50~51ms). 이후에는 `[MemoryWorker]` / `[CpuWorker]` 만 남는다.

→ 분석: [reports/04_scheduling_analysis.md](reports/04_scheduling_analysis.md)

---

## 5. 정리 — 환경변수 조합표

아래 "실측 결과" 열은 2026-08-26 수집분 그대로다 (근거: [evidence/](evidence/) 각 파일 §4).

| 실험 | MEMORY_LIMIT | CPU_MAX_OCCUPY | MULTI_THREAD_ENABLE | 실측 결과 |
| ---- | ------------ | -------------- | ------------------- | --------- |
| OOM Before | **256** | 10 | false | **0m34s** 후 exit 137 — `[MemoryGuard] Memory limit exceeded (275MB >= 256MB)` + `Self-terminating process …` |
| OOM After | **512** | 10 | false | 관측 상한 **3m00s** 까지 종료 없음 (배너 `[ OK ]` → 누수 시나리오 미선택) |
| CPU Before | 512 | **80** | false | **0m30s** 후 exit 143 — `[CpuWorker] CPU Threshold Violated! (53.72…%)` |
| CPU After | 512 | **10** | false | 관측 상한 **3m00s** 까지 종료 없음 (`Peak reached → cooldown` 17회 순환) |
| Deadlock Before | 512 | 10 | **true** | 부팅 **약 9초** 후 무응답 (PID 생존, 210초 뒤에도 무변화 → 수동 SIGTERM) |
| Deadlock After | 512 | 10 | **false** | **3m00s** 정상 가동 (로그 전진, `TIME+` 전진) |
| 스케줄링 추론 | 512 | 10 | **false** | `[Scheduler]` A→B→C 회전 21이벤트 / 간격 50~51ms |

**파라미터 스윕 (임계점 확정)**

| 변수 | 값별 실측 |
| ---- | --------- |
| `MEMORY_LIMIT` (CPU=10 고정) | 100 → 13초(137) / 256 → 34초(137) / 512 → >180초 생존 |
| `CPU_MAX_OCCUPY` (MEM=512 고정) | 95 → 34초(143) / 80 → 30초(143) / 60 → 43초(143) / 50 → >122초 생존 / 10 → >180초 생존 |

→ CPU 쪽 트립 지점은 설정값과 무관하게 모두 50% 직후(58.93 / 53.72 / 57.39%)다. **종료선은 고정 50%** 이며,
따라서 유효한 조치는 상향이 아니라 **하향**이다.
